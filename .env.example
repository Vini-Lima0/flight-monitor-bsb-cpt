# Copie este arquivo para ".env" para rodar localmente (o .env nunca é commitado).
# Em produção (GitHub Actions), os mesmos nomes viram GitHub Secrets — ver README.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
